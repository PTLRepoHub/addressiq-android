# Required: the SDK depends on AndroidX (Compose, androidx.*) + Play Services.
android.useAndroidX=true
android.enableJetifier=false

# Larger heap for the Kotlin/Compose compile in CI.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
